name: circular_loader
description: "A new Flutter plugin project."
version: 0.0.1
homepage: "circular_loader"

environment:
  sdk: ^3.7.2
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: any
  font_awesome_flutter: any
  flutter_widget_from_html: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  plugin:
    platforms:
      some_platform:
        pluginClass: somePluginClass
